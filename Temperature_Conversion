#include <iostream>

/*Celsius > Fehrenheit Conversion Formula: 
  C=(F-32)/1.8 */
int main() {

double tempf;
double tempc;
/*Line(s) 7-8: Create's 2 new variables to store the date of the temperatures.*/

std::cout<<"What is the temperature in Fehrenheit?\n";
std::cin>>tempf;
/*Line(s) 12-12: Asks for user input and inserts the given data into an assigned variable.*/
  
tempc=(tempf-32)/1.8;
/*Line(s) 15: Uses the input in tempf to solve the the equation and insert the return into the tempc variable.*/

std::cout<<"The temperature is "<<tempc<<" degrees Celsius.\n";
/*Line(s) 18: Displays the final outout of tempc along with a string of fitting words.*/  
  
}
